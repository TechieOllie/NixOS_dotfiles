keys:
  - &the-entertaining-nios-vm age1hsaqjyj4mtmdyyqtetjn0twlfeppn0vxla724tl9uy702lv0qp7sxmejq5
  - &the-entertaining-nios-laptop age182ymkfkp0gx8lu66gd4c5dfhq8632p42898h5a4p20xdln75j4fq5ssa6y
  - &the-entertaining-nios-desktop age189gntjuuenp0jgc4knr2dmg290psrfu469s02zfu9zwdlk0f9cksazeysc
  # PLACEHOLDER — replace with the real public key from
  # `age-keygen -o ~/.config/sops/age/inotmac.txt`, run by the operator (this
  # agent does not hold that key and did not generate one). Everything below
  # that references it is inert config, not a working recipient, until this
  # line is replaced with the real age1... value. See the secrets runbook
  # left in this commit's message / hosts/inotmac/secrets.nix for the exact
  # steps.
  - &inotmac age1REPLACE-WITH-A-REAL-AGE-PUBLIC-KEY-FOR-INOTMAC
creation_rules:
  - path_regex: hosts/the-entertaining-nios-vm/secrets/.*\.yaml$
    key_groups:
      - age:
          - *the-entertaining-nios-vm
  - path_regex: hosts/the-entertaining-nios-laptop/secrets/.*\.yaml$
    key_groups:
      - age:
          - *the-entertaining-nios-laptop
  - path_regex: hosts/the-entertaining-nios-desktop/secrets/.*\.yaml$
    key_groups:
      - age:
          - *the-entertaining-nios-desktop
  - path_regex: hosts/inotmac/secrets/.*\.yaml$
    key_groups:
      - age:
          - *inotmac
